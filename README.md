# `setcap` rock

Rock packing the `setcap` utility from the `libcap2-bin` package.

This rock is mean to be interchangeable with the upstream
[kubernetes/release](https://github.com/kubernetes/release/tree/master/images/build)
build images.